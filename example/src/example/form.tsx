import * as React from 'react';

import { FormProps } from '../../..';

export default function Form({
  defaultValues,
  resolver,
  render,
  onSubmit,
  onBack,
}: FormProps<{}>) {
  return <></>;
}
